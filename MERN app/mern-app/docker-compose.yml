version: '3'
services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    restart: always

  client:
    build: ./client
    ports:
      - "80:80"
    depends_on:
      - server
    restart: always
